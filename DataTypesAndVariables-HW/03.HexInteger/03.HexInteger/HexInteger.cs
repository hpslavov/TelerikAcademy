using System;
class Program
{
    static void Main()
    {
        //Declare an integer variable and assign it with the value 254 in hexadecimal format (0x##).
        int hexInteger = 0xFE;
        Console.WriteLine("The decimal value of FE is " + hexInteger);
    }
}
